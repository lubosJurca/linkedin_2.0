

const HeaderOptions = ({children}) => {
  return (
    <a className="cursor-pointer text-[#666666] flex flex-col justify-center items-center hover:bg-gray-100 sm:p-2 rounded" href="#">{children}</a>
  )
}

export default HeaderOptions